angular
  .module("app")
  .controller("signUpFormController",["$scope", function($scope){

      function signUpFormController() {
        const SafeCharactersRegex = /[^.\n \/]*[\w\d\*\@!?.,+]{8,64}/;





    function testFormComplete(required_fields, completed_fields){
      if()
    }

    function arSum(arr){
      return arr.reduce(function(acc, val) { acc + val}, 0);
    }
    }
    ));
